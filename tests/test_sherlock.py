import sherlock_scraper
from nodes import OnlineAccount
"""
def test_lookup():
    results = sherlock_scraper.lookup("torvalds")
    assert all(isinstance(r, OnlineAccount) for r in results)
    assert any(r.site == "GitHub" for r in results)
"""
